{
  "prd": {
    "problemDefinition": "```json\n{\n  \"prd\": {\n    \"problemDefinition\": \"非エンジニア（研究者・専門職）がプロダクト開発に踏み出せない根本原因は、ターミナル・Git・CI/CDといった技術ツールの操作手順以前に、「なぜそれが必要なのか」という概念的理解が欠如していることにある。現状の解決手段はエンジニアによる1対1のマンツーマン指導だが、横についていれば操作できても自走には至らず（F4）、かつスケールしない（F3）。AIによる個別最適化なしには、同じ課題が繰り返し同じコストで発生し続け、プロダクト開発の民主化が進まない。具体的には、GitHubアカウント作成からリポジトリ管理・コマンドライン操作・プルリクエスト・CI/CD構築までの一連のプロセスを、難しさを体感させずに習得させることが解決すべき問題である。\",\n    \"targetUser\": \"PC基本操作に不慣れな非エンジニア専門職（例：細胞培養スペシャリスト、研究者）。ターミナルやGitHubの概念を持たず、外注では自分たちのニーズを要件として言語化すること自体が困難なため自作ニーズがある。技術的な難しさを感じた瞬間に離脱する傾向があり、30分以内に達成感を得られないとモチベーションを失う。自走できるようになることを最終目標とするが、現状は手順の模倣にとどまっている層。\",\n    \"jobsToBeDone\": [\n      \"「なぜGitが必要なのか」を自分の言葉・経験に紐づけて理解し、操作手順を暗記ではなく意味として習得したい\",\n      \"GitHubアカウント作成からCI/CD構築までの一連のセットアップを、エンジニアに頼らず自分一人で完走したい\",\n      \"自分のPCスキルレベルに合わせた説明を受け、恥ずかしい思いをせずに学習を進めたい\",\n      \"30分程度の時間で「できた」という成功体験を得て、次のステップ（プロダクト要件定義・実装）へ進む自信をつけたい\"\n    ],\n    \"coreFeatures\": [\n      {\n        \"name\": \"AIレベル推定・個別最適化エンジン\",\n        \"description\": \"ユーザーとの会話（最大5ターン）の中で、PC操作歴・失敗体験・職業ドメイン知識を自動推定し、以降の全説明文・例示・比喩をそのユーザープロファイルに合わせてリアルタイムに生成する。例：研究者には「実験ノートのバージョン管理」としてGitを説明。レベルは「初心者／中級者／上級者」の3段階に分類し、各ステップの説明粒度・スキップ可否をDBに保存して継続利用できるようにする。\",\n        \"priority\": \"must\",\n        \"acceptanceCriteria\": [\n          \"初回会話開始から5ターン以内にユーザーレベル（初心者／中級者／上級者）が自動分類され、分類結果がDBに保存されること\",\n          \"分類されたレベルに応じて、次ステップの説明文がAI APIから動的に生成されること（ハードコードされたテンプレート文字列での代替は不可）\",\n          \"ユーザーが「わからない」「もう少し詳しく」と入力した場合、同一ステップの説明がより平易な比喩（職業ドメインに紐づいたもの）に切り替わって再生成されること\",\n          \"レベル推定結果と会話ログが実データとしてDBに保存され、次回ログイン時に引き継がれること\",\n          \"データ経路が実物のDB接続およびAI APIとの接続であること（モックレスポンスでの実装は完了とみなさない）\"\n        ],\n        \"edgeCases\": [\n          \"エッジケース: AI APIがタイムアウト（5秒超）した場合、ユーザーに「AIが応答できていません、再試行してください」と明示し、モック説明文で補完してはならない\",\n          \"エッジケース: ユーザーが5ターン経過後もレベル判定不能な回答（「わかりません」のみ）を繰り返した場合、デフォルトで「初心者」に分類してセッションを継続する\",\n          \"エッジケース: ユーザーが途中でブラウザを閉じた場合、会話ログとレベル推定の中間状態がDBに保存され、再アクセス時に中断箇所から再開できること\",\n          \"エッジケース: 同一アカウントが複数デバイスから同時アクセスした場合、最後に書き込んだセッションのレベルを正として保存し、競合を通知する\",\n          \"エッジケース: AI APIのレスポンスが有害・無関係なコンテンツを含む場合、フィルタリングして表示せず「説明を生成できませんでした」と表示する\"\n        ]\n      },\n      {\n        \"name\": \"概念体感シナリオ：「失敗→Gitの必要性」体験フロー\",\n        \"description\": \"Git操作の説明に入る前に、ユーザーに仮想ファイル（ブラウザ上のサンドボックス環境）を提供し、「上書き保存して前のバージョンに戻れなくなる」失敗体験をシミュレートする。その後「この問題を解決するのがGit」という流れで概念を接続する。体験はブラウザ内完結のサンドボックスで行い、ユーザーのPC環境を汚染しない。体験ログ（どの操作で詰まったか）はDBに保存し、AIが次の説明をパーソナライズするインプットとして使用する。\",\n        \"priority\": \"must\",\n        \"acceptanceCriteria\": [\n          \"ブラウザ上のサンドボックス内でユーザーがファイル編集・上書き保存・バージョン喪失の3ステップを実際に操作できること（UIの表示のみでは不可）\",\n          \"「バージョンが失われた」状態をユーザーが確認した後、「Gitがあればこうなっていた」という復元デモが同一画面内で実行されること\",\n          \"体験完了後、ユーザーが「Gitが必要だと思いますか？」の設問に回答し、その回答がDBに保存されること\",\n          \"体験中にユーザーが詰まった操作ステップ（クリック停止時間3秒以上）がイベントログとしてDBに記録されること\",\n          \"データ経路が実物のDB接続であること（ハードコードされた配列やスタブAPIでの実装は完了とみなさない）\"\n        ],\n        \"edgeCases\": [\n          \"エッジケース: サンドボックス環境のロードに失敗した場合、「環境を準備できませんでした」と表示し、代替として概念説明テキストのみを表示する（体験完了扱いにはしない）\",\n          \"エッジケース: ユーザーがサンドボックスで意図的に全ファイルを削除した場合、サンドボックスをリセットして再試行を促し、セッションを終了させない\",\n          \"エッジケース: モバイルデバイスからアクセスした場合、サンドボックス操作がタッチ操作に対応していない旨を表示し、PC推奨メッセージを出す\",\n          \"エッジケース: 体験フローを途中でスキップしようとした場合、「概念理解なしに次に進むと自走が困難になる」旨の警告を1回表示した上でスキップを許可する\"\n        ]\n      },\n      {\n        \"name\": \"ステップ別ガイド付きターミナル操作インターフェース\",\n        \"description\": \"ブラウザ内に埋め込まれた仮想ターミナル（WebSocketで接続されたサーバーサイドシェル）上で、GitHubアカウント作成→リポジトリ作成→git init/add/commit/push→プルリクエスト作成→CI/CD設定（GitHub Actions YAMLの自動生成）の各ステップを、AIが次のコマンドを自然言語で説明しながらガイドする。各コマンドの実行結果を解析し、エラーの場合はAIが原因と修正方法を日本語で即座に提示する。コマンドのコピペを防ぐため、入力補助はサジェストまでとし、最終的にユーザーが自分でEnterを押す設計とする。\",\n        \"priority\": \"must\",\n        \"acceptanceCriteria\": [\n          \"ブラウザ内仮想ターミナルから実際のGitHub API（OAuth認証済み）に対してリポジトリ作成コマンドが実行でき、GitHubアカウント上に実際のリポジトリが作成されること\",\n          \"git add・commit・pushが仮想ターミナルから実行され、実際のGitHubリポジトリにコミット履歴が反映されること\",\n          \"ターミナルのコマンド実行結果（stdoutおよびstderr）がリアルタイムでブラウザに表示され、エラー発生時に5秒以内にAIによる日本語のエラー説明が表示されること\",\n          \"GitHub Actions YAMLファイルが自動生成されてリポジトリにpushされ、GitHub上でCI/CDワークフローが実際にトリガーされること\",\n          \"各ステップ完了時刻がDBに記録され、合計経過時間が画面上に表示されること\",\n          \"データ経路が実物のGitHub API・サーバーサイドシェル・DB接続であること（モックターミナルでの実装は完了とみなさない）\"\n        ],\n        \"edgeCases\": [\n          \"エッジケース: GitHubのOAuth認証が失敗した場合、「GitHub認証に失敗しました。再認証してください」と表示し、認証フローを再起動する\",\n          \"エッジケース: WebSocket接続が切断された場合、自動再接続を3回試みた後に「接続が切れました」を表示し、それまでの進捗はDBから復元する\",\n          \"エッジケース: ユーザーが危険なコマンド（rm -rf /など）を入力した場合、実行前にブロックして「このコマンドは実行できません」と警告する\",\n          \"エッジケース: GitHub APIのレート制限（5000req/h）に達した場合、「GitHub APIの制限に達しました。XX分後に再試行してください」と残り時間を表示する\",\n          \"エッジケース: CI/CDのワークフロー実行がGitHub側でエラーになった場合、GitHub APIからエラーを取得して日本語で表示し、修正方法をAIが提示する\",\n          \"エッジケース: ユーザーが同一コマンドを3回連続でミスした場合、AIが「一緒に確認しましょう」と介入し、コマンドを分解して1要素ずつ説明する\"\n        ]\n      },\n      {\n        \"name\": \"学習進捗トラッキングと達成証明\",\n        \"description\": \"GitHubアカウント作成・リポジトリ作成・最初のコミット・プルリクエスト・CI/CD設定の5つのマイルストーンを定義し、各マイルストーン達成を実際のGitHub APIのレスポンスで検証してDBに記録する。30分タイマーを画面に常時表示し、達成状況を可視化する。全マイルストーン達成時に「達成証明カード」を生成し、共有可能なURLを発行する。未達成で30分を超えた場合も「どこまで進んだか」を記録し、次回セッション継続を促す。\",\n        \"priority\": \"must\",\n        \"acceptanceCriteria\": [\n          \"5つのマイルストーン（GitHubアカウント作成確認・リポジトリ作成確認・初回コミット確認・PR作成確認・CI/CD実行確認）が、それぞれGitHub API経由の実レスポンスで検証され、DBに完了フラグが保存されること\",\n          \"画面上のタイマーがセッション開始から経過秒数をリアルタイムで表示し、30分（1800秒）超過時に視覚的な通知（色変更など）が表示されること\",\n          \"全5マイルストーン達成後、ユニークURLを持つ達成証明カードが生成され、そのURLにアクセスすると達成内容と達成時刻が実DBデータから表示されること\",\n          \"30分超過後も未達成マイルストーンが残っている場合、次回ログイン時に「前回の続きから」ボタンが表示され、未完了ステップから再開できること\",\n          \"データ経路が実物のGitHub APIおよびDB接続であること（マイルストーン達成をフロントエンド側の変数で管理するだけの実装は完了とみなさない）\"\n        ],\n        \"edgeCases\": [\n          \"エッジケース: GitHub APIでアカウント存在確認が取れない場合（アカウント削除済みなど）、該当マイルストーンを未達成に戻しユーザーに通知する\",\n          \"エッジケース: ユーザーが達成証明URLを他人に共有された場合、個人情報（メールアドレスなど）を含まず、GitHubユーザー名と達成内容のみを表示する\",\n          \"エッジケース: セッション中にブラウザがクラッシュした場合、タイマーの経過時間をDBから復元し、クラッシュ前の進捗を正確に反映する\",\n          \"エッジケース: 同一ユーザーが複数回セッションをリセットして30分タイマーを再スタートした場合、最初のセッション開始時刻を基準に累計時間をDBで管理する\"\n        ]\n      },\n      {\n        \"name\": \"オンボーディング前ヒアリング（Deep Form）\",\n        \"description\": \"学習開始前に5問以内の会話形式ヒアリングを実施し、ユーザーの職業・PC使用歴・過去のプログラミング経験・作りたいプロダクトのイメージを収集する。回答はDBに保存され、AIレベル推定エンジンの初期プロファイルとして使用される。ヒアリングはフォーム形式ではなくチャット形式で行い、ユーザーが自由文で回答できる。回答内容からAIが「あなたにはこの順番で教えます」という個別カリキュラムの概要を提示してから学習を開始する。\",\n        \"priority\": \"must\",\n        \"acceptanceCriteria\": [\n          \"5問以内のチャット形式ヒアリングが完了し、回答がDBに構造化データ（職業・PC歴・プログラミング経験・プロダクトイメージ）として保存されること\",\n          \"ヒアリング回答を受けてAI APIが個別カリキュラム概要（ステップ数・強調する概念・スキップするステップ）を動的に生成してユーザーに提示すること\",\n          \"カリキュラム概要がDBに保存され、実際の学習ステップの順序・内容に反映されること\",\n          \"ヒアリング完了から学習開始まで30秒以内に遷移できること（AI生成の待機時間を含む）\",\n          \"データ経路が実物のAI APIおよびDB接続であること（固定のカリキュラムテンプレートを表示するだけの実装は完了とみなさない）\"\n        ],\n        \"edgeCases\": [\n          \"エッジケース: ユーザーがヒアリングをすべてスキップした場合、デフォルトプロファイル（初心者・一般職）でカリキュラムを生成し、スキップしたことをDBに記録する\",\n          \"エッジケース: ヒアリング中にAI APIが失敗した場合、「AIが応答できませんでした」と表示し、デフォルトカリキュラムで学習を開始するか再試行するかをユーザーに選択させる\",\n          \"エッジケース: ユーザーが個人情報（氏名・所属機関名など）をヒアリングに入力した場合、AI処理前にマスキングしてDBには個人特定情報を保存しない\",\n          \"エッジケース: 2回目以降のログイン時に再度ヒアリングを求められた場合、「前回の回答を引き継ぎますか？」と確認してDBから復元する\"\n        ]\n      }\n    ],\n    \"nonGoals\": [\n      \"プロダクトの要件定義・実装（Deep Form・exe.devの機能領域）はスコープ外とし、本プロダクトはGit/CI/CDの習得完了までを担う\",\n      \"GitHubアカウント作成後のプロダクトコードの中身（アプリケーション開発）の指導は行わない\",\n      \"対面・音声・動画を使った指導機能の提供（テキストチャットインターフェースのみ）\",\n      \"チーム開発・複数人コラボレーション（フォーク・ブランチ戦略など）の高度なGit操作の指導\",\n      \"GitHub以外のバージョン管理システム（GitLab・Bitbucket）への対応\",\n      \"スマートフォンのみを使った学習への最適化（PC使用を前提とする）\",\n      \"学習コンテンツの管理者向け編集UI（コンテンツはAI動的生成のみ）\"\n    ],\n    \"userFlows\": [\n      {\n        \"name\": \"初回ユーザー：ゼロからCI/CD完走フロー\",\n        \"steps\": [\n          \"1. トップページにアクセス→「今すぐ始める」ボタンをクリック→メールアドレスまたはGoogleアカウントでサインアップ\",\n          \"2. チャット形式ヒアリング開始（最大5問）→職業・PC歴・経験・作りたいものを自由入力\",\n          \"3. AI がヒアリング回答を解析し「あなた向けカリキュラム概要」を提示→「このカリキュラムで始める」ボタンをクリック\",\n          \"4. 概念体感シナリオ起動→サンドボックス上でファイル上書き失敗体験→「バージョン管理が必要だと理解した」を確認→次へ\",\n          \"5. GitHubアカウント作成ガイド→AIが職業に合わせた説明でGitHub.comへ誘導→アカウント作成完了をGitHub OAuthで確認→マイルストーン①達成\",\n          \"6. 仮想ターミナル起動→AIガイドに従い git init・リポジトリ作成コマンドを入力→GitHubに実リポジトリ作成確認→マイルストーン②達成\",\n          \"7. サンプルファイルをサンドボックスに生成→git add・git commit・git pushをターミナルで実行→GitHubに実コミット反映確認→マイルストーン③達成\",\n          \"8. AIがプルリクエストの概念を職業比喩で説明→ブランチ作成・変更・PR作成をターミナルで実行→GitHub上にPRが実際に作成されることを確認→マイルストーン④達成\",\n          \"9. GitHub Actions YAMLをAIが自動生成→ターミナルでpush→GitHub上でCI/CDワークフロー実行確認→マイルストーン⑤達成\",\n          \"10. 全マイルストーン達成→達成証明カード生成・URL発行→「次はプロダクト要件定義（Deep Form）へ」のリンクを表示\"\n        ]\n      },\n      {\n        \"name\": \"途中離脱ユーザー：再開フロー\",\n        \"steps\": [\n          \"1. トップページにアクセス→ログイン→「前回の続きから再開」ボタンが表示される\",\n          \"2. 前回の進捗（達成済みマイルストーンと停止ステップ）がDBから復元され画面に表示される\",\n          \"3. 停止したステップの直前からAIガイドが再開（ヒアリング結果・レベル推定はDB引き継ぎ）\",\n          \"4. タイマーは累計経過時間を引き継いで継続表示\",\n          \"5. 詰まっていたステップで再度つまずいた場合、AIが前回とは異なる比喩・説明を自動選択して提示\",\n          \"6. 残マイルストーンを完走→達成証明カード発行\"\n        ]\n      },\n      {\n        \"name\": \"ヘルプ要求フロー：コマンドエラー発生時\",\n        \"steps\": [\n          \"1. ユーザーが仮想ターミナルでコマンドを実行→エラー（stderr）が表示される\",\n          \"2. 5秒以内にAIが「このエラーの意味」と「次に入力すべきコマンド（説明付き）」を日本語でチャット欄に表示\",\n          \"3. ユーザーが「もっと詳しく」と入力した場合、AIがさらに分解した説明を再生成\",\n          \"4. 同一エラーが3回連続した場合、AIが「一緒に確認しましょう」と介入し、コマンドを1要素ずつ入力させるモードに切り替え\",\n          \"5. エラー解決後、次のステップへの移行案内が自動表示\"\n        ]\n      }\n    ],\n    \"qualityRequirements\": {\n      \"functionalSuitability\": {\n        \"description\": \"5つのマイルストーン（GitHubアカウント作成・リポジトリ作成・コミット・PR・CI/CD）の全パスが、実際のGitHub APIおよびDBとの接続を通じて正確に機能すること。AIによる説明生成がユーザーのレベルプロファイルに基づいて適切に変化すること。\",\n        \"criteria\": [\n          \"5つの全マイルストーンが実際のGitHub APIレスポンスで検証され、DBに正確に記録される成功率が95%以上であること\",\n          \"AIレベル推定が初心者・中級者・上級者の3分類において、同一ユーザーへの再推定時に同一分類を返す一貫性が90%以上であること\",\n          \"仮想ターミナルで実行されたgit pushが、60秒以内にGitHub上の実リポジトリに反映されること\",\n          \"CI/CD YAMLの自動生成がGitHub Actionsで構文エラーなく実行されること（構文エラー率1%未満）\"\n        ]\n      },\n      \"performanceEfficiency\": {\n        \"description\": \"学習継続に支障をきたすような待機時間をなくし、特にAI応答・ターミナルリアルタイム表示・マイルストーン検証において応答速度を保証する。\",\n        \"criteria\": [\n          \"AIによる説明文生成（初回レスポンス）は95パーセンタイルで5秒以内であること\",\n          \"仮想ターミナルへのコマンド入力からstdout表示までのレイテンシが500ミリ秒以内であること（WebSocketラウンドトリップ含む）\",\n          \"GitHub APIを使ったマイルストーン検証が10秒以内に完了すること\",\n          \"同時接続100セッション時に、上記の応答時間基準が維持されること\",\n          \"ブラウザ上の仮想ターミナルのメモリ使用量が1セッションあたり200MB以下であること\"\n        ]\n      },\n      \"compatibility\": {\n        \"description\": \"非エンジニアユーザーが使用するあらゆる���準的なPCブラウザ環境で動作し、外部サービス（GitHub API・AI API）との相互運用を保証する。\",\n        \"criteria\": [\n          \"Chrome・Safari・Firefox・Edgeの最新版（リリース後6ヶ月以内）でブラウザ仮想ターミナルおよび全UI機能が動作すること\",\n          \"GitHub API v4（GraphQL）およびv3（REST）の仕様変更に対して、APIバージョンを明示的に固定してリクエストすること\",\n          \"AI APIの応答フォーマット変更時に、フォールバックパーサーが機能して学習フローが止まらないこと\",\n          \"Windows・macOS・Linux（Ubuntu）のChrome最新版でE2Eテストが全パス通ること\"\n        ]\n      },\n      \"usability\": {\n        \"description\": \"「難しいと感じた瞬間に離脱する」ユーザー特性に対応し、PC操作に不慣れなユーザーが説明書なしで学習を開始・継続・完了できること。\",\n        \"criteria\": [\n          \"初回ユーザーがトップページから最初のAIヒアリング回答を送信するまでの操作を、説明書なしで2分以内に完了できること（ユーザーテスト5名中4名）\",\n          \"各ステップで次に何をすべきかのアクションが常に1つだけ明示され、ユーザーが迷って3秒以上クリックしない割合が80%以上であること（操作ログによる計測）\",\n          \"エラー発生時のAIメッセージが「何がまずかったか」「次に何をすべきか」の2点を必ず含み、技術用語を使わない（中学生レベルの語彙）こと\",\n          \"カラーコントラスト比がWCAG 2.1 AA基準（4.5:1以上）を満たすこと\",\n          \"キーボードのみで全操作が完了できること（タブキーによるフォーカス移動を含む）\"\n        ]\n      },\n      \"reliability\": {\n        \"description\": \"学習中のセッション断・サービス障害・外部API障害が発生しても、ユーザーの進捗データが失われず、障害復旧後に継続できること。\",\n        \"criteria\": [\n          \"月間稼働率99.0%以上（月間ダウンタイム7.2時間以内）\",\n          \"WebSocket切断発生時に自動再接続を3回試み、再接続後にターミナルセッションをDBから復元できること\",\n          \"GitHub APIが503を返した場合、エクスポネンシャルバックオフ（1秒・2秒・4秒）で再試行し、ユーザーには「GitHubが混雑しています、自動で再試行中」と表示すること\",\n          \"セッション中のDBへの書き込みが失敗した場合、ローカルストレージに一時保存し、書き込み成功後にDBに同期されること\",\n          \"AI APIが完全停止した場合、学習フローが「AI未実装」と明示した状態で停止し、モ",
    "targetUser": "",
    "jobsToBeDone": [],
    "coreFeatures": [],
    "nonGoals": [],
    "userFlows": [],
    "metrics": []
  }
}
